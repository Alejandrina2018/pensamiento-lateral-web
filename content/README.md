# Content

`final-copy.md` (pending) will hold the client-approved copy referenced in
`CLAUDE.md` #34 — Home, Investigación, Datos, Automatizaciones e IA,
Empresas, Instituciones, Quiénes somos, Casos, Insights, Pymes, Contacto.

Once it's added, that copy gets wired into the corresponding sections/pages
as-is — no rewriting, no additions (`CLAUDE.md` #34). Any typo,
inconsistency or duplicate text found in it gets flagged before anything is
changed, per the same section.

This folder is a staging area for static copy today. When Sanity is
integrated (`CLAUDE.md` #17), Insights, Casos, Press and Authors move into
CMS-managed documents matching the shapes already defined in
`src/types/content.ts`.
